var qlik;var scp;var qvobjects={};var curApp;var tpid="dashboard.html";var config = {	//GEN	host: "localhost",	port : "4848",	isSecure :false,	//ENDGEN	prefix: "/"};require.config( {	waitSeconds:0,	baseUrl: ( config.isSecure ? "https://" : "http://" ) + config.host + (config.port ? ":" + config.port: "") + config.prefix + "resources"} );function qlikize(){	var tot=$( ".qvobject" ).length;	var cpt=0;	$( ".qvobject" ).each( function () {		var qvid = $( this ).data( "qvid" );		var curNode=$( this );		if(curNode.attr('stateLoad')!='done'){			if(typeof(curApp)!='undefined'){				qvobjects[qvid]=curApp.getObject( this, qvid );				cpt++;				curNode.attr('stateLoad','done');				if(cpt==tot){					hideLoad();				}			}			}		else{			cpt++;		}			});	if(cpt==tot){		hideLoad();	}}function hideLoad(){	$('#preloader-logo2').fadeOut();	$('#preloader-icon2').fadeOut();	$('#preloader2').delay(350).fadeOut(function(){		qlik.resize();	});}function pager($scope){	scp=$scope;	$scope.selections=false;	$scope.curTemplate='dashboard.html';	$scope.getTemplate=function(){		return $scope.curTemplate;	}	$scope.setTemplate=function(tp){		tpid=tp;		$scope.curTemplate=tp;	}	$scope.ready=function(){		panelize("#template");		excludeBS();		qlikize();	}	$scope.hideSel=function(){		fixSel();		$scope.selections=!($scope.selections);	};	$scope.back=function(detail){		animateHide($('#'+detail),$('#sum'));	};	$scope.showDetails=function(detail){		animateHide($('#sum'),$('#'+detail));	};}function excludeBS(){	$("body").find('*').each(function(){		if(!$(this).hasClass('qvobject'))			$(this).addClass('qv');	});}function fixSel(){	$('.list').height('0');	$('.buttons-end').hide();}function animateShow(toShow) {    var $elem = toShow;	toShow.show();	qlik.resize();    $({deg: 90}).animate({deg: 0}, {        duration: 500,        step: function(now) {            $elem.css({                transform: 'rotateY(' + now + 'deg)'            });			if(now==0)qlik.resize();        }    });}function animateHide(toHide,toShow) {    var $elem = toHide;    $({deg: 0}).animate({deg: 90}, {        duration: 500,        step: function(now) {            $elem.css({                transform: 'rotateY(' + now + 'deg)'            });			if(now==90){				toHide.hide();				animateShow(toShow)			}        }    });}function initReady(){		$('.qv-selection-toolbar').livequery(function() {		   $(this).hide();		   var top=parseInt($(this).css('top').replace('px',''));		   $(this).css('top',top+ $(document).scrollTop());		   $(this).show();		});		excludeBS();		$(window).scroll(function() {			$("#CurrentSelections" ).addClass("hideSel");			clearTimeout($.data(this, 'scrollTimer'));			$.data(this, 'scrollTimer', setTimeout(function() {				$("#CurrentSelections" ).removeClass("hideSel");			}, 2000));		});			$("#all").show();		require( ["js/qlik"], function ( qlikview ){			qlik=qlikview;			qlikview.getAppList(function(reply) {				$.each(reply, function(key, value) {					if (value.qDocName === "Executive Dashboard" || value.qDocName === "Executive Dashboard.qvf"){						var id=value.qDocName.indexOf(".qvf")!=-1?value.qDocName:value.qDocId;						var app = qlikview.openApp(id, config);						curApp=app;						qlikview.setOnError( function ( error ) {							alert( error.message );						} );						qlikize();					}					});			},config);			});	}//window.onload = function(){   initReady();//};